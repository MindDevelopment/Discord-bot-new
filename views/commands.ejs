<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bot Commands</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Zorg ervoor dat je eigen stylesheet wordt ingeladen -->
</head>
<body>
    <h1>Bot Commands</h1>
    <p>Hier is een lijst van alle beschikbare commando's:</p>
    <table>
        <thead>
            <tr>
                <th>Naam</th>
                <th>Beschrijving</th>
            </tr>
        </thead>
        <tbody>
            <% commands.forEach(command => { %>
                <tr>
                    <td><%= command.name %></td>
                    <td><%= command.description %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
